" Panels and windows {{{
    " ListToggle {
        let g:lt_height = 10
    " }
    " Gundo {
        let g:gundo_width = 60
        let g:gundo_preview_height = 20
        let g:gundo_right = 0
        let g:gundo_preview_bottom = 1
        let g:gundo_preview_statusline = "Undo preview"
        let g:gundo_tree_statusline = "Undo graph"
    " }
" }}}
" vim:set foldmarker={{{,}}} foldlevel=0 foldmethod=marker:
